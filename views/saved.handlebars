    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-collapse-bar" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/">Mongo Scraper</a>
            </div>
            <div class="collapse navbar-collapse" id="bs-collapse-bar">
                <ul class="nav navbar-nav">
                    <li class="active">
                        <a href="/">Home
                            <span class="sr-only">(current)</span>
                        </a>
                    </li>
                    <li>
                        <a id="saved" href="/saved">Saved Articles</a>
                    </li>
                </ul>
                <form class="navbar-form navbar-left" action="/api/fetch" method="get">
                    <button class="btn btn-warning scrape">Scrape New Articles!</button>
                </form>
            </div>
        </div>
    </nav>

	<div class="main-img-container">
		<img class="main-img" src="assets/img/nyt-pappers.jpg" alt="header-image-nyt-papers" />
	</div>

    <div class="savedArt articles">
    	<ul>
    		{{#each articles }}
    		<div class="panel panel-default">
    		  <div class="panel-heading">
    		    <h3 class="panel-title">{{title}}</h3>
    		  </div>
    		  <div class="panel-body">
    		    <a class="artLink" href={{link}}>{{link}}</a>
                <div>
                    <br/>
                    <button data-id={{_id}} class="btn btn-warning saved-buttons"  id="addCheckNote" data-toggle="modal" data-target="#noteModal" data-value="{{this._id}}">Article Notes</button>
                    <button data-id={{_id}} class="btn btn-secondary removeSaved">Remove from Saved</button>
                </div>
    		  </div>
    		</div>
    		<br />


      <div id="noteModal" class="modal fade"  tabindex="-1" role="dialog" aria-labelledby="noteModalLabel" aria-hidden="true">
          <div class="modal-dialog" role="document">
              <div class="modal-content">
                  <div class="modal-header">
                      <h4 class="modal-title" id=""><b>Notes:</b></h4>
                      <div>
                          <ul id="notesBody">

                          </ul>
                      </div>
                  </div>
                  <div class="modal-body">
                      <div class="form-group">

                          <label for="exampleTextarea">Enter New Note</label>
                          <textarea class="form-control" id="notestext" rows="3"></textarea>
                      </div>
                      <div></div>
                  </div>
                  <div class="modal-footer">
                      <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                      <button data-id={{_id}}  data-dismiss="modal" id="saveButton" class="btn btn-warning savenote">Save changes</button>
                  </div>
              </div>
          </div>
      </div>

      {{/each}}
    </ul>

    </div>